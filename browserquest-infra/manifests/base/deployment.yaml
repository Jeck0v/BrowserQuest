apiVersion: apps/v1
kind: Deployment
metadata:
  name: browserquest
  namespace: browserquest
spec:
  replicas: 1
  selector:
    matchLabels:
      app: browserquest
  template:
    metadata:
      labels:
        app: browserquest
    spec:
      containers:
        - name: browserquest
          image: jeck0v/browserquest:lastest
          ports:
            - containerPort: 80
              name: http
            - containerPort: 8080
              name: websocket
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 200m
